{
  "name": "@funboxteam/markdown-lint",
  "version": "0.1.0",
  "description": "Markdown code style linter.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint --cache --ext .js ./",
    "postcommit": "git update-index --again",
    "precommit": "lint-staged",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "markdown",
    "linter"
  ],
  "devDependencies": {
    "@funboxteam/eslint-config": "4.8.0",
    "eslint": "5.6.1",
    "husky": "1.1.1",
    "lint-staged": "7.3.0"
  },
  "lint-staged": {
    "linters": {
      "*.js": [
        "eslint --fix --cache",
        "git add"
      ]
    },
    "renderer": "silent"
  }
}
